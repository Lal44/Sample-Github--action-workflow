name: "Hellow world workflow"

on:
    push:
        branches:
            - master
            - producction

jobs:
    hello_world:
        runs-on: ubuntu-latest
        steps:
            - name: "Print hello world"
              run: echo "hello world"
